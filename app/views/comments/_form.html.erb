<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :body, "Write your comment" %><br />
    <%= f.text_field :body %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
