<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class="field">
    
    <%= f.text_field :body, :style => 'width: 320px;' 'height:150px;'%>
  </div>
  <br />
  <div class="actions">
    <%= f.submit "Submit"%>

  </div>
<% end %>
